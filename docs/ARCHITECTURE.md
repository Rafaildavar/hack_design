# 🏗️ Архитектура проекта hac_

## 📐 Общая архитектура системы

```
┌─────────────────────────────────────────────────────────────────┐
│                        CLIENT LAYER                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────┐         ┌──────────────┐                    │
│  │   Web App    │         │ Telegram Bot │                    │
│  │  (Next.js)   │         │  (Python)    │                    │
│  │              │         │              │                    │
│  │  - Landing   │         │  - Commands  │                    │
│  │  - Dashboard │         │  - Inline KB │                    │
│  │  - Forms     │         │  - Messages  │                    │
│  └──────┬───────┘         └──────┬───────┘                    │
│         │                         │                            │
└─────────┼─────────────────────────┼────────────────────────────┘
          │                         │
          │ HTTP/REST               │ HTTP/REST
          │                         │
┌─────────▼─────────────────────────▼────────────────────────────┐
│                      API GATEWAY LAYER                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│                    ┌──────────────────┐                       │
│                    │   FastAPI Backend │                       │
│                    │   (Port 8000)     │                       │
│                    │                   │                       │
│                    │  - REST API       │                       │
│                    │  - CORS           │                       │
│                    │  - Auth           │                       │
│                    │  - Validation     │                       │
│                    └─────────┬─────────┘                       │
│                              │                                  │
└──────────────────────────────┼──────────────────────────────────┘
                               │
                    ┌──────────┴──────────┐
                    │                     │
        ┌───────────▼──────────┐  ┌───────▼──────────┐
        │   BUSINESS LOGIC     │  │   DATA LAYER     │
        │                      │  │                  │
        │  - AI Service        │  │  - SQLAlchemy    │
        │  - Image Service     │  │  - SQLite/Postgres│
        │  - Schedule Service  │  │  - Models        │
        │  - Lesson Service    │  │  - Migrations    │
        └───────────┬──────────┘  └──────────────────┘
                    │
        ┌───────────┴───────────┐
        │                        │
┌───────▼────────┐      ┌────────▼────────┐
│  EXTERNAL APIs │      │  AUTOMATION     │
│                │      │                 │
│  - OpenAI      │      │  - n8n          │
│  - Mistral     │      │  - Workflows    │
│  - Flux/DALL-E │      │  - Schedules    │
│  - Telegram    │      └─────────────────┘
└────────────────┘
```

---

## 🔧 Детальная архитектура Backend

```
┌──────────────────────────────────────────────────────────────┐
│                    BACKEND ARCHITECTURE                      │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│  LAYER 1: API ENDPOINTS (FastAPI Routers)                   │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  /api/assistant  →  AssistantRouter                          │
│  /api/schedule   →  ScheduleRouter                           │
│  /api/lesson     →  LessonRouter                             │
│  /api/image      →  ImageRouter                              │
│  /api/tg/send    →  TelegramRouter                           │
│  /api/n8n/webhook → N8nWebhookRouter                        │
│                                                              │
│  Каждый роутер:                                              │
│  • Валидация входных данных (Pydantic)                      │
│  • Вызов сервисного слоя                                     │
│  • Обработка ошибок                                          │
│  • Форматирование ответа                                     │
│                                                              │
└───────────────────────────┬──────────────────────────────────┘
                            │
┌───────────────────────────▼──────────────────────────────────┐
│  LAYER 2: SERVICE LAYER (Business Logic)                    │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────────┐  ┌─────────────────┐                  │
│  │  AIService      │  │  ImageService   │                  │
│  │                 │  │                 │                  │
│  │  • chat()       │  │  • generate()   │                  │
│  │  • _openai()    │  │  • _flux()      │                  │
│  │  • _mistral()   │  │  • _dalle()     │                  │
│  └─────────────────┘  └─────────────────┘                  │
│                                                              │
│  ┌─────────────────┐  ┌─────────────────┐                  │
│  │ ScheduleService │  │  LessonService  │                  │
│  │                 │  │                 │                  │
│  │  • generate()   │  │  • create()     │                  │
│  │  • format()     │  │  • format()     │                  │
│  │  • validate()   │  │  • save()       │                  │
│  └─────────────────┘  └─────────────────┘                  │
│                                                              │
│  ┌─────────────────┐  ┌─────────────────┐                  │
│  │ TelegramService │  │  DatabaseService│                  │
│  │                 │  │                 │                  │
│  │  • send()       │  │  • save_user()  │                  │
│  │  • format()     │  │  • get_schedule│                  │
│  │  • error()      │  │  • save_lesson() │                  │
│  └─────────────────┘  └─────────────────┘                  │
│                                                              │
└───────────────────────────┬──────────────────────────────────┘
                            │
┌───────────────────────────▼──────────────────────────────────┐
│  LAYER 3: DATA ACCESS LAYER (SQLAlchemy ORM)                │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ User Model   │  │Schedule Model│  │ Lesson Model │     │
│  │              │  │              │  │              │     │
│  │ • id         │  │ • id         │  │ • id         │     │
│  │ • telegram_id│  │ • user_id    │  │ • user_id    │     │
│  │ • username   │  │ • subjects   │  │ • topic      │     │
│  │ • created_at │  │ • schedule   │  │ • content    │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ Image Model  │  │ ChatHistory  │  │ Preferences  │     │
│  │              │  │              │  │              │     │
│  │ • id         │  │ • id         │  │ • id         │     │
│  │ • user_id    │  │ • user_id    │  │ • user_id    │     │
│  │ • prompt     │  │ • message    │  │ • settings   │     │
│  │ • image_url  │  │ • response   │  │ • timezone   │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                              │
│  Database: SQLite (dev) / PostgreSQL (prod)                  │
│  Migrations: Alembic                                        │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔄 Потоки данных (Data Flow)

### 1. Генерация расписания

```
User (Web/Telegram)
    │
    │ POST /api/schedule
    │ {subjects: [...], hours_per_week: 10}
    ▼
FastAPI Router (schedule.py)
    │
    │ Валидация данных
    ▼
ScheduleService
    │
    │ Формирование промпта для AI
    ▼
AIService
    │
    │ Запрос к OpenAI/Mistral
    ▼
OpenAI API / Mistral API
    │
    │ JSON ответ с расписанием
    ▼
AIService
    │
    │ Парсинг и форматирование
    ▼
ScheduleService
    │
    │ Сохранение в БД
    ▼
DatabaseService → SQLite/Postgres
    │
    │ Возврат результата
    ▼
FastAPI Router
    │
    │ JSON ответ
    ▼
User (Web/Telegram)
```

### 2. Генерация конспекта

```
User (Telegram Bot)
    │
    │ /lesson "Математика: интегралы"
    ▼
Telegram Bot Handler
    │
    │ POST /api/lesson
    │ {topic: "Математика: интегралы", format: "summary"}
    ▼
FastAPI Router (lesson.py)
    │
    │ Валидация
    ▼
LessonService
    │
    │ Формирование промпта
    ▼
AIService
    │
    │ Запрос к AI
    ▼
OpenAI/Mistral
    │
    │ Конспект
    ▼
LessonService
    │
    │ Сохранение в БД
    ▼
DatabaseService
    │
    │ Форматирование для Telegram
    ▼
Telegram Bot
    │
    │ Отправка сообщения
    ▼
User (Telegram)
```

### 3. Ежедневный дайджест (через n8n)

```
n8n Schedule Trigger
    │
    │ Каждый день в 9:00
    ▼
n8n HTTP Request
    │
    │ GET /api/users/active
    ▼
FastAPI Backend
    │
    │ Список активных пользователей
    ▼
n8n Loop
    │
    │ Для каждого пользователя:
    │ GET /api/schedule/{user_id}
    │ GET /api/lessons/recent/{user_id}
    ▼
FastAPI Backend
    │
    │ Данные пользователя
    ▼
n8n Function Node
    │
    │ Форматирование дайджеста
    ▼
n8n Telegram Node
    │
    │ Отправка в Telegram
    ▼
Users (Telegram)
```

---

## 🗄️ Схема базы данных

```
┌─────────────────────────────────────────────────────────────┐
│                    DATABASE SCHEMA                           │
└─────────────────────────────────────────────────────────────┘

┌──────────────┐
│    users     │
├──────────────┤
│ id (PK)      │◄─────┐
│ telegram_id  │      │
│ username     │      │
│ first_name   │      │
│ last_name    │      │
│ created_at   │      │
│ last_active  │      │
│ is_active    │      │
└──────────────┘      │
                      │
        ┌─────────────┼─────────────┐
        │             │             │
        │             │             │
┌───────▼──────┐ ┌───▼────────┐ ┌──▼──────────┐
│  schedules   │ │  lessons   │ │   images    │
├──────────────┤ ├────────────┤ ├─────────────┤
│ id (PK)      │ │ id (PK)    │ │ id (PK)     │
│ user_id (FK) │ │ user_id(FK)│ │ user_id(FK) │
│ subjects     │ │ topic      │ │ prompt      │
│ schedule_data│ │ content     │ │ image_url   │
│ hours_week   │ │ format      │ │ style       │
│ created_at   │ │ level       │ │ created_at  │
│ updated_at   │ │ created_at  │ └─────────────┘
└──────────────┘ └────────────┘
                      │
        ┌─────────────┼─────────────┐
        │             │             │
┌───────▼──────┐ ┌───▼────────┐ ┌──▼──────────────┐
│chat_history  │ │preferences │ │                 │
├──────────────┤ ├────────────┤ │                 │
│ id (PK)      │ │ id (PK)    │ │                 │
│ user_id (FK) │ │ user_id(FK)│ │                 │
│ message      │ │ default_   │ │                 │
│ response     │ │  format    │ │                 │
│ context      │ │ digest_time│ │                 │
│ created_at   │ │ timezone   │ │                 │
└──────────────┘ └────────────┘ └─────────────────┘
```

---

## 🔌 Интеграции с внешними сервисами

```
┌─────────────────────────────────────────────────────────────┐
│              EXTERNAL SERVICES INTEGRATION                     │
└─────────────────────────────────────────────────────────────┘

Backend (FastAPI)
    │
    ├───► OpenAI API
    │     • GPT-3.5/GPT-4 для текста
    │     • DALL-E для изображений
    │     • Endpoint: https://api.openai.com/v1/
    │
    ├───► Mistral API
    │     • Mistral-tiny/small для текста
    │     • Альтернатива OpenAI
    │     • Endpoint: https://api.mistral.ai/v1/
    │
    ├───► Flux API
    │     • Генерация изображений
    │     • Endpoint: https://api.flux.ai/v1/
    │
    ├───► Telegram Bot API
    │     • Отправка сообщений
    │     • Получение обновлений
    │     • Endpoint: https://api.telegram.org/bot{token}/
    │
    └───► n8n Webhooks
          • Прием данных от n8n
          • Отправка данных в n8n
          • Endpoint: http://n8n:5678/webhook/
```

---

## 🚀 Деплой и инфраструктура

```
┌─────────────────────────────────────────────────────────────┐
│                    DEPLOYMENT ARCHITECTURE                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    DOCKER CONTAINERS                         │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Backend    │  │     n8n      │  │   Postgres   │     │
│  │  Container   │  │  Container   │  │  Container   │     │
│  │              │  │              │  │              │     │
│  │ Port: 8000   │  │ Port: 5678   │  │ Port: 5432   │     │
│  │ Image:       │  │ Image:       │  │ Image:       │     │
│  │ python:3.11  │  │ n8nio/n8n    │  │ postgres:15  │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│                                                              │
│  Volumes:                                                    │
│  • backend_data:/app/data                                    │
│  • n8n_data:/home/node/.n8n                                 │
│  • postgres_data:/var/lib/postgresql/data                   │
│                                                              │
└──────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    PRODUCTION (Vercel/Render)                │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  Frontend (Next.js) → Vercel                                 │
│  Backend (FastAPI) → Render/Railway                          │
│  Database → Supabase/Neon                                    │
│  n8n → Self-hosted или n8n.cloud                             │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 🔐 Безопасность

```
┌─────────────────────────────────────────────────────────────┐
│                    SECURITY LAYERS                           │
└─────────────────────────────────────────────────────────────┘

1. API Security
   • CORS настройки (только разрешенные домены)
   • Rate limiting (защита от DDoS)
   • API ключи в .env (не в коде)
   • Валидация всех входных данных

2. Database Security
   • SQL injection защита (SQLAlchemy ORM)
   • Пароли в переменных окружения
   • Миграции с проверкой

3. Telegram Bot Security
   • Верификация токена
   • Проверка подписи webhook
   • Ограничение доступа по user_id

4. External API Security
   • API ключи в .env
   • HTTPS для всех запросов
   • Таймауты для запросов
```

---

## 📊 Мониторинг и логирование

```
┌─────────────────────────────────────────────────────────────┐
│              MONITORING & LOGGING                             │
└─────────────────────────────────────────────────────────────┘

Backend Logs:
  • FastAPI access logs
  • Error logs (stderr)
  • AI API request/response logs
  • Database query logs

n8n Logs:
  • Workflow execution logs
  • Error notifications
  • Webhook activity

Telegram Bot Logs:
  • Command usage statistics
  • Error tracking
  • User activity

Metrics to Track:
  • API response times
  • AI API costs
  • Database query performance
  • User engagement
```

---

## 🎯 Ключевые принципы архитектуры

1. **Separation of Concerns**
   - API слой отделен от бизнес-логики
   - Сервисы независимы друг от друга
   - Данные изолированы в моделях

2. **Scalability**
   - Stateless API (легко масштабировать)
   - Асинхронные операции
   - Кэширование где возможно

3. **Maintainability**
   - Чистый код
   - Документация
   - Типизация (Pydantic, SQLAlchemy)

4. **Reliability**
   - Обработка ошибок на всех уровнях
   - Retry логика для внешних API
   - Валидация данных

5. **Performance**
   - Асинхронные запросы
   - Оптимизация БД запросов
   - Кэширование частых запросов

---

## 📝 Следующие шаги

1. ✅ Архитектура спроектирована
2. ⏳ Реализация API эндпоинтов
3. ⏳ Настройка БД и моделей
4. ⏳ Интеграция с AI сервисами
5. ⏳ Тестирование всех компонентов
6. ⏳ Деплой и мониторинг


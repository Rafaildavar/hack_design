# ⚡ Быстрый старт для команды

## 🎯 Для тимлида (первые 30 минут)

1. **Клонировать репозиторий**
   ```bash
   git clone https://github.com/Rafaildavar/-_-.git
   cd "ХАКАТОН ДИЗАЙН"
   ```

2. **Проверить Docker**
   ```bash
   docker --version
   docker-compose --version
   ```

3. **Создать .env файл**
   ```bash
   # Скопируйте пример (если есть)
   # Или создайте вручную с минимальными настройками
   ```

4. **Запустить все сервисы**
   ```bash
   docker-compose up -d
   ```

5. **Проверить работу**
   - Backend: http://localhost:8000/health
   - n8n: http://localhost:5678
   - Swagger: http://localhost:8000/docs

## 🎨 Для фронтенд-разработчика

1. **Инициализировать Next.js**
   ```bash
   cd frontend
   npx create-next-app@latest . --typescript --tailwind --app --no-src-dir
   ```

2. **Установить shadcn/ui**
   ```bash
   npx shadcn-ui@latest init
   ```

3. **Запустить dev сервер**
   ```bash
   npm run dev
   ```

4. **Создать базовые страницы**
   - `/` - лендинг
   - `/app` - панель ассистента
   - `/schedule` - расписание
   - `/lessons` - уроки
   - `/media` - медиа
   - `/newsletter` - рассылка

## 🔧 Для бэкенд-разработчика

1. **Проверить структуру**
   ```bash
   cd backend
   ls -la
   ```

2. **Установить зависимости**
   ```bash
   pip install -r requirements.txt
   ```

3. **Запустить локально**
   ```bash
   uvicorn app.main:app --reload
   ```

4. **Протестировать эндпоинты**
   - Откройте http://localhost:8000/docs
   - Протестируйте `/health`
   - Протестируйте `/api/assistant`

## 🤖 Для интеграций (n8n + Telegram)

1. **Создать Telegram бота**
   - Откройте @BotFather
   - `/newbot`
   - Сохраните токен

2. **Открыть n8n**
   - http://localhost:5678
   - Логин: admin
   - Пароль: admin

3. **Создать первый workflow**
   - Webhook → HTTP Request → Telegram

4. **Протестировать бота**
   - Отправьте `/start` боту

## 🎨 Для дизайнера

1. **Изучить структуру проекта**
   - Прочитать TEAM_TASKS.md
   - Понять все страницы

2. **Создать референсы**
   - Figma или набросок
   - Цветовая палитра
   - Компоненты

3. **Начать проверку**
   - Проверять работу фронтенда
   - Давать фидбек

---

## ✅ Чеклист готовности (через 1 час)

- [ ] Docker контейнеры запущены
- [ ] Backend отвечает на запросы
- [ ] n8n открывается
- [ ] Frontend проект создан
- [ ] Telegram бот создан
- [ ] UI референсы готовы
- [ ] Команда понимает задачи

**Если все галочки стоят - можно начинать разработку! 🚀**


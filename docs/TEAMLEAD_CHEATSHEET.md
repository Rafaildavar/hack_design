# 🎯 Шпаргалка для тимлида

## ⚡ Первые 30 минут

```bash
# 1. Клонировать репозиторий
git clone https://github.com/Rafaildavar/-_-.git
cd "ХАКАТОН ДИЗАЙН"

# 2. Проверить Docker
docker --version
docker-compose --version

# 3. Запустить все сервисы
docker-compose up -d

# 4. Проверить работу
curl http://localhost:8000/health
# Должно вернуть: {"status": "healthy"}

# 5. Открыть в браузере
# Backend: http://localhost:8000/docs
# n8n: http://localhost:5678
```

## 🔍 Проверка работы сервисов

```bash
# Статус контейнеров
docker-compose ps

# Логи backend
docker-compose logs -f backend

# Логи n8n
docker-compose logs -f n8n

# Перезапуск сервиса
docker-compose restart backend
```

## 🧪 Тестирование API

```bash
# Health check
curl http://localhost:8000/health

# Assistant
curl -X POST http://localhost:8000/api/assistant \
  -H "Content-Type: application/json" \
  -d '{"message": "Привет!"}'

# Schedule
curl -X POST http://localhost:8000/api/schedule \
  -H "Content-Type: application/json" \
  -d '{"subjects": ["Математика"], "hours_per_week": 10}'
```

## 📋 Чекпоинты (напоминание)

- **1:00** - Базовый каркас готов
- **3:00** - Основные фичи работают
- **5:00** - Все страницы готовы
- **7:00** - Финальная полировка
- **8:00** - ГОТОВО К ЗАЩИТЕ

## 🚨 Частые проблемы

### Порт занят
```bash
# Найти процесс
lsof -i :8000
# Убить процесс
kill -9 <PID>
```

### Docker не запускается
```bash
# Перезапустить Docker Desktop
# Или
docker-compose down
docker-compose up -d
```

### n8n не открывается
```bash
# Проверить логи
docker-compose logs n8n
# Перезапустить
docker-compose restart n8n
```

## 📞 Коммуникация с командой

### Вопросы для стендапа (каждые 30 мин)
1. Что сделано?
2. Что делаешь сейчас?
3. Есть блокеры?

### Когда нужна помощь тимлида
- ❌ Не могу запустить проект
- ❌ Критический баг
- ❌ Застрял больше 30 минут
- ❌ Интеграция не работает

## 🎯 Приоритеты

1. **Критично** - Все сервисы запускаются
2. **Важно** - API работает
3. **Важно** - Интеграции работают
4. **Желательно** - Все фичи реализованы

## 📚 Документы команды

- `docs/TEAM_TASKS.md` - Детальное ТЗ
- `docs/CHECKPOINTS.md` - Чекпоинты по времени
- `docs/SETUP_GUIDE.md` - Инструкция по запуску
- `docs/QUICK_START.md` - Быстрый старт

## ✅ Финальный чеклист (перед защитой)

- [ ] Все сервисы запускаются одной командой
- [ ] Все API эндпоинты работают
- [ ] Telegram бот отвечает
- [ ] n8n workflows работают
- [ ] Frontend открывается и работает
- [ ] Документация готова
- [ ] Презентация готова
- [ ] Демо работает без сбоев

---

**Удачи! Ты справишься! 🚀**

